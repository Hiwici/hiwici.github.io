/**
 * TODO: Maintain the select Catan hex on the map, and the state of UI
 *
 * A Pinia store for managing the map state of the application.
 *
 * @store useMapStore
 */
export const useMapStore = defineStore('map', {
  state: () => ({
    //
  }),
  getters: {
    //
  },
  actions: {
    //
  },
})
